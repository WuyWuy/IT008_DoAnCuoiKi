<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <Color x:Key="Color.SoftWater">#E0F2FE</Color>
    <SolidColorBrush x:Key="Brush.SoftWater" Color="{StaticResource Color.SoftWater}"/>

    <Color x:Key="Color.SunGlare">#F0F9FF</Color>

    <Color x:Key="Color.DeepOcean">#0EA5E9</Color>
    <SolidColorBrush x:Key="Brush.DeepOcean" Color="{StaticResource Color.DeepOcean}"/>

    <Color x:Key="Color.FloatingLeaf">#10B981</Color>

    <SolidColorBrush x:Key="Brush.TextPrimary" Color="#0F172A"/>
    <SolidColorBrush x:Key="Brush.TextSecondary" Color="#64748B"/>
    <LinearGradientBrush x:Key="Brush.ZenBackground" StartPoint="0,0" EndPoint="1,1">
        <GradientStop Color="#FFFFFF" Offset="0.0"/>
        <GradientStop Color="#F0F9FF" Offset="0.4"/>
        <GradientStop Color="#E0F2FE" Offset="0.7"/>
        <GradientStop Color="#D1FAE5" Offset="1.0"/>
    </LinearGradientBrush>

    <Style x:Key="WaterFlowBorderStyle" TargetType="Border">
    <Setter Property="CornerRadius" Value="24"/>
    <Setter Property="BorderThickness" Value="3"/>
    <Setter Property="SnapsToDevicePixels" Value="True"/>

    <Setter Property="Background">
        <Setter.Value>
            <LinearGradientBrush StartPoint="0,0" EndPoint="1,1">
                <GradientStop Color="#90FFFFFF" Offset="0.0"/>
                <GradientStop Color="#40F0F9FF" Offset="0.5"/>
                <GradientStop Color="#50E0F2FE" Offset="1.0"/>
            </LinearGradientBrush>
        </Setter.Value>
    </Setter>

    <Setter Property="BorderBrush">
        <Setter.Value>
            <LinearGradientBrush StartPoint="0,0" EndPoint="1,1">
                <LinearGradientBrush.RelativeTransform>
                    <RotateTransform CenterX="0.5" CenterY="0.5" Angle="0"/>
                </LinearGradientBrush.RelativeTransform>
                <GradientStop Color="#00FFFFFF" Offset="0.0"/>
                <GradientStop Color="#38BDF8" Offset="0.3"/> <GradientStop Color="#00FFFFFF" Offset="0.5"/>
                <GradientStop Color="#34D399" Offset="0.7"/> <GradientStop Color="#00FFFFFF" Offset="1.0"/>
            </LinearGradientBrush>
        </Setter.Value>
    </Setter>

    <Setter Property="Effect">
        <Setter.Value>
            <DropShadowEffect Color="#7DD3FC" BlurRadius="15" ShadowDepth="4" Direction="315" Opacity="0.3"/>
        </Setter.Value>
    </Setter>

    <Setter Property="RenderTransform">
        <Setter.Value>
            <ScaleTransform ScaleX="1" ScaleY="1"/>
        </Setter.Value>
    </Setter>

    <Style.Triggers>
        <EventTrigger RoutedEvent="Loaded">
            <BeginStoryboard>
                <Storyboard>
                    <DoubleAnimation Storyboard.TargetProperty="(Border.BorderBrush).(Brush.RelativeTransform).(RotateTransform.Angle)"
                                     From="0" To="360" Duration="0:0:6" RepeatBehavior="Forever"/>
                </Storyboard>
            </BeginStoryboard>
        </EventTrigger>

        <Trigger Property="IsMouseOver" Value="True">
            <Setter Property="Background">
                <Setter.Value>
                    <LinearGradientBrush StartPoint="0,0" EndPoint="1,1">
                        <GradientStop Color="#CCFFFFFF" Offset="0.0"/> <GradientStop Color="#60BAE6FD" Offset="1.0"/> </LinearGradientBrush>
                </Setter.Value>
            </Setter>
            
            <Setter Property="Effect">
                <Setter.Value>
                    <DropShadowEffect Color="#38BDF8" BlurRadius="25" ShadowDepth="0" Opacity="0.5"/>
                </Setter.Value>
            </Setter>
            
            <Setter Property="RenderTransform">
                <Setter.Value>
                    <ScaleTransform ScaleX="1.02" ScaleY="1.02" CenterX="150" CenterY="75"/> 
                    </Setter.Value>
            </Setter>
        </Trigger>
    </Style.Triggers>
</Style>

    <Style x:Key="ZenTextStyle" TargetType="TextBlock">
        <Setter Property="FontFamily" Value="Segoe UI Light"/>
        <Setter Property="Foreground" Value="{StaticResource Brush.TextPrimary}"/>
        <Setter Property="FontSize" Value="16"/>
        <Setter Property="VerticalAlignment" Value="Center"/>
        <Setter Property="HorizontalAlignment" Value="Center"/>
    </Style>

</ResourceDictionary>