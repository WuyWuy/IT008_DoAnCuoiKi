<Window x:Class="QuanLyCaPhe.Views.Components.JetMoonMessageBox"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Title="Thông báo" 
        Height="220" Width="420"
        WindowStyle="None"
        ResizeMode="NoResize"
        AllowsTransparency="True"
        Background="Transparent"
        WindowStartupLocation="CenterScreen"
        Topmost="True">

    <Window.Resources>
        <Style x:Key="MsgPrimaryButton" TargetType="Button">
            <Setter Property="Height" Value="36"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Grid>
                            <Border x:Name="bd" CornerRadius="8" SnapsToDevicePixels="True">
                                <Border.Background>
                                    <LinearGradientBrush StartPoint="0,0" EndPoint="1,0">
                                        <GradientStop Color="#0EA5E9" Offset="0.0"/>
                                        <GradientStop Color="#0284C7" Offset="1.0"/>
                                    </LinearGradientBrush>
                                </Border.Background>
                            </Border>
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Grid>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="bd" Property="Effect">
                                    <Setter.Value>
                                        <DropShadowEffect Color="#0EA5E9" BlurRadius="20" ShadowDepth="0" Opacity="0.8"/>
                                    </Setter.Value>
                                </Setter>
                                <Setter Property="RenderTransform">
                                    <Setter.Value>
                                        <ScaleTransform ScaleX="1.02" ScaleY="1.02" CenterX="50" CenterY="18"/>
                                    </Setter.Value>
                                </Setter>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter Property="RenderTransform">
                                    <Setter.Value>
                                        <ScaleTransform ScaleX="0.98" ScaleY="0.98" CenterX="50" CenterY="18"/>
                                    </Setter.Value>
                                </Setter>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="MsgSecondaryButton" TargetType="Button">
            <Setter Property="Height" Value="36"/>
            <Setter Property="Foreground" Value="#64748B"/>
            <Setter Property="Background" Value="White"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Grid>
                            <Border x:Name="bd" CornerRadius="8" Background="White" BorderBrush="#CBD5E1" BorderThickness="1" SnapsToDevicePixels="True"/>
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Grid>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="bd" Property="Background" Value="#F1F5F9"/>
                                <Setter TargetName="bd" Property="BorderBrush" Value="#94A3B8"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>

    <Border Style="{DynamicResource LiquidGlassStyle}" 
            Background="AliceBlue"
            MouseLeftButtonDown="Border_MouseLeftButtonDown">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <Grid Grid.Row="0" Margin="20,15,20,5">
                <StackPanel Orientation="Horizontal" HorizontalAlignment="Left">
                    <Path Fill="#0C4A6E" Data="M12,2C12,2 12,2 12,2C17.5,2.05 22,6.5 22,12C21.95,17.5 17.5,22 12,22C6.5,21.95 2,17.5 2,12C2.05,6.5 6.5,2 12,2M6.14,14.63L7.71,15.2C8.63,12.87 11.66,12.18 13.57,14.47L14.73,13.5C12.1,10.37 7.41,11.41 6.14,14.63M10.84,5.43L9.67,6.4C12.3,9.53 17,8.47 18.27,5.27L16.7,4.7C15.77,7.03 12.75,7.72 10.84,5.43Z" 
                          Width="16" Height="16" Margin="0,2,8,0" VerticalAlignment="Center"/>

                    <TextBlock x:Name="txtTitle" 
                               Text="Thông báo"
                               FontFamily="Segoe UI Light"
                               FontSize="20"
                               Foreground="#0C4A6E"
                               FontWeight="SemiBold"/>
                </StackPanel>
            </Grid>

            <Grid Grid.Row="1" Margin="25,10,25,10" VerticalAlignment="Center">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>

                <Border x:Name="bdrIcon" Width="40" Height="40" CornerRadius="20" Background="#E0F2FE" Margin="0,0,15,0">
                    <Path x:Name="iconMain" Fill="#0EA5E9" Stretch="Uniform" Width="20" Height="20" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                </Border>

                <TextBlock x:Name="txtMessage" Grid.Column="1"
                           Text="Nội dung thông báo sẽ hiển thị ở đây."
                           TextWrapping="Wrap"
                           FontSize="15"
                           FontFamily="Segoe UI"
                           Foreground="#334155"
                           VerticalAlignment="Center"
                           LineHeight="22"/>
            </Grid>

            <Grid Grid.Row="2" Margin="0,0,0,20">
                <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                    <Button x:Name="btnCancel" Content="Hủy bỏ" Style="{StaticResource MsgSecondaryButton}" Width="100" Margin="0,0,15,0" Click="btnCancel_Click" Visibility="Collapsed"/>
                    <Button x:Name="btnOK" Content="Đồng ý" Style="{StaticResource MsgPrimaryButton}" Width="100" Click="btnOK_Click"/>
                </StackPanel>
            </Grid>
        </Grid>
    </Border>
</Window>