<Window x:Class="QuanLyCaPhe.Views.Admin.DetailWindow.UserDetailWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:QuanLyCaPhe.Views.Admin.DetailWindow"
        mc:Ignorable="d"
        Title="Thông Tin Nhân Viên" Height="653" Width="450"
        WindowStartupLocation="CenterScreen" ResizeMode="NoResize"
        Background="{StaticResource BackgroundLight}">

    <Border Style="{StaticResource LiquidGlassStyle}" Padding="20" Margin="10">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <TextBlock Text="THÔNG TIN NHÂN VIÊN" 
                       HorizontalAlignment="Center" 
                       FontSize="22" FontWeight="Bold" 
                       Foreground="{StaticResource PrimaryColor}" 
                       Margin="0,0,0,20"/>

            <StackPanel Grid.Row="1" VerticalAlignment="Top">

                <TextBlock Text="Họ và tên:" FontWeight="SemiBold" Margin="0,10,0,5" Foreground="#1E1B4B"/>
                <TextBox x:Name="txtFullName" IsReadOnly="False" Style="{StaticResource GlassTextStyleInput}"/>

                <TextBlock Text="Email:" FontWeight="SemiBold" Margin="0,10,0,5" Foreground="#1E1B4B"/>
                <TextBox x:Name="txtEmail" Style="{StaticResource GlassTextStyleInput}"/>

                <TextBlock Text="Số điện thoại:" FontWeight="SemiBold" Margin="0,10,0,5" Foreground="#1E1B4B"/>
                <TextBox x:Name="txtPhone" Style="{StaticResource GlassTextStyleInput}"/>

                <TextBlock Text="Địa chỉ:" FontWeight="SemiBold" Margin="0,10,0,5" Foreground="#1E1B4B"/>
                <TextBox x:Name="txtAddress" Style="{StaticResource GlassTextStyleInput}"/>

                <TextBlock Text="Giới tính:" FontWeight="SemiBold" Margin="0,10,0,5" Foreground="#1E1B4B"/>
                <ComboBox x:Name="cboGender" Style="{StaticResource ComboBoxStyle}">
                    <ComboBoxItem Content="Nam" Tag="Male" IsSelected="True"/>
                    <ComboBoxItem Content="Nữ" Tag="Female"/>
                </ComboBox>

                <TextBlock Text="Chức vụ:" FontWeight="SemiBold" Margin="0,10,0,5" Foreground="#1E1B4B"/>
                <ComboBox x:Name="cboRole" Style="{StaticResource ComboBoxStyle}">
                    <ComboBoxItem Content="Admin" Tag="Admin"/>
                    <ComboBoxItem Content="Staff" Tag="Staff" IsSelected="True"/>
                </ComboBox>

                <StackPanel x:Name="pnlPassword">
                    <TextBlock Text="Mật khẩu khởi tạo:" FontWeight="SemiBold" Margin="0,10,0,5" Foreground="#1E1B4B"/>
                    <PasswordBox x:Name="txtPassword" Style="{StaticResource MaterialPasswordBoxStyle}"/>
                </StackPanel>
                <TextBox x:Name="txtEmail_Copy" Style="{StaticResource GlassTextStyleInput}"/>
            </StackPanel>

            <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,20,0,0">

                <Button Content="Lưu lại" Width="100"
                        Style="{StaticResource GlassButtonStyle}"
                        Click="BtnSave_Click"/>

                <Button Content="Hủy bỏ" Width="100" Margin="0,0,10,0"
                        Style="{StaticResource GlassDeleteButtonStyle}"
                        Click="BtnCancel_Click"/>
            </StackPanel>
        </Grid>
    </Border>
</Window>