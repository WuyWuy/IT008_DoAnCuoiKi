<Window x:Class="QuanLyCaPhe.Views.Admin.DetailWindow.BillDetailWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Chi Tiết Hóa Đơn" 
        MinHeight="600" Height="auto" Width="600"
        WindowStartupLocation="CenterScreen" ResizeMode="NoResize"
        Background="{StaticResource BorderStroke}">
    <Border Style="{StaticResource DynamicGlassStyle}" Padding="25" Margin="15">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <StackPanel Grid.Row="0" Margin="0,0,0,20">
                <TextBlock Text="CHI TIẾT HÓA ĐƠN" 
                           HorizontalAlignment="Center" 
                           FontFamily="Segoe UI Light" FontSize="26" FontWeight="Bold" 
                           Foreground="#0C4A6E"/>
                <Separator Background="#0C4A6E" Opacity="0.2" Margin="50,5,50,0"/>
            </StackPanel>

            <Grid Grid.Row="1" Margin="0,0,0,20">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="20"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>

                <StackPanel Grid.Column="0">
                    <TextBlock Text="Mã hóa đơn" Style="{StaticResource FontBody}" FontWeight="SemiBold" Foreground="#0C4A6E"/>
                    <TextBox x:Name="txtId" IsReadOnly="True" Style="{StaticResource GlassTextStyleInput}" Background="#F3F4F6"/>

                    <TextBlock Text="Ngày Check-In" Style="{StaticResource FontBody}" FontWeight="SemiBold" Margin="0,10,0,0" Foreground="#0C4A6E"/>
                    <TextBox x:Name="txtDateCheckIn" IsReadOnly="True" Style="{StaticResource GlassTextStyleInput}" Background="#F3F4F6"/>
                </StackPanel>

                <StackPanel Grid.Column="2">
                    <TextBlock Text="Bàn" Style="{StaticResource FontBody}" FontWeight="SemiBold" Foreground="#0C4A6E"/>
                    <TextBox x:Name="txtTableName" IsReadOnly="True" Style="{StaticResource GlassTextStyleInput}" Background="#F3F4F6"/>

                    <TextBlock Text="Thu ngân" Style="{StaticResource FontBody}" FontWeight="SemiBold" Margin="0,10,0,0" Foreground="#0C4A6E"/>
                    <TextBox x:Name="txtUser" IsReadOnly="True" Style="{StaticResource GlassTextStyleInput}" Background="#F3F4F6"/>
                </StackPanel>
            </Grid>

            <Border Grid.Row="2" Background="White" CornerRadius="8" Padding="1" BorderBrush="#E5E7EB" BorderThickness="1">
                <DataGrid x:Name="dgBillInfo" 
                          Style="{StaticResource CardStyleDataGridStyle}"
                          AutoGenerateColumns="False" 
                          IsReadOnly="True" Background="Transparent" BorderThickness="0">
                    <DataGrid.Columns>
                        <DataGridTextColumn Header="Tên món" Binding="{Binding TenMon}" Width="*"/>
                        <DataGridTextColumn Header="SL" Binding="{Binding SoLuong}" Width="50"/>
                        <DataGridTextColumn Header="Đơn giá" Binding="{Binding DonGia, StringFormat={}{0:N0}}" Width="90"/>
                        <DataGridTextColumn Header="Thành tiền" Binding="{Binding ThanhTien, StringFormat={}{0:N0}}" Width="100" FontWeight="Bold" Foreground="#0C4A6E"/>
                    </DataGrid.Columns>
                </DataGrid>
            </Border>

            <Grid Grid.Row="3" Margin="0,20,0,0">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>

                <Border Grid.Column="0" Width="90" Height="90" 
                        Background="White" CornerRadius="8" BorderBrush="#E5E7EB" BorderThickness="1">
                    <Grid>
                        <TextBlock Text="QR AREA" HorizontalAlignment="Center" VerticalAlignment="Center" FontSize="10" FontWeight="Bold" Foreground="#64748B"/>
                    </Grid>
                </Border>

                <StackPanel Grid.Column="1" Margin="20,0,0,0" VerticalAlignment="Bottom">
                    <Grid Margin="0,0,0,15">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>

                        <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" Grid.Column="0" Margin="0,0,15,0">
                            <TextBlock Text="Giảm giá:" VerticalAlignment="Center" Margin="0,0,10,0" Foreground="#64748B"/>
                            <TextBox x:Name="txtDiscount" Width="60" Style="{StaticResource GlassTextStyleInput}" TextAlignment="Center" IsReadOnly="True" Background="#F3F4F6"/>
                            <TextBlock Text="%" VerticalAlignment="Center" Margin="5,0,0,0" Foreground="#64748B"/>
                        </StackPanel>

                        <StackPanel Grid.Column="1" Orientation="Horizontal">
                            <TextBlock Text="TỔNG:" FontWeight="Bold" FontSize="16" VerticalAlignment="Center" Margin="0,0,10,0" Foreground="#0C4A6E"/>
                            <TextBlock x:Name="lblTotalPrice" Text="0 VNĐ" FontWeight="Bold" FontSize="22" Foreground="#EF4444" VerticalAlignment="Center"/>
                        </StackPanel>
                    </Grid>

                    <Button Content="Đóng" Width="120" HorizontalAlignment="Right"
                            Style="{StaticResource GlassDeleteButtonStyle}"
                            Click="BtnClose_Click"/>
                </StackPanel>
            </Grid>
        </Grid>
    </Border>
</Window>