<Window x:Class="QuanLyCaPhe.Views.Admin.DetailWindow.ProductDetailWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:QuanLyCaPhe.Views.Admin"
        mc:Ignorable="d"
        Title="Chi Tiết Món Ăn" Height="400" Width="450"
        WindowStartupLocation="CenterScreen" ResizeMode="NoResize"
        Background="{StaticResource BackgroundLight}">

    <Border Style="{StaticResource LiquidGlassStyle}" Padding="20" Margin="10">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <TextBlock Text="CHI TIẾT MÓN ĂN" 
                       HorizontalAlignment="Center" 
                       FontSize="22" FontWeight="Bold" 
                       Foreground="{StaticResource PrimaryColor}" 
                       Margin="0,0,0,20"/>

            <StackPanel Grid.Row="1">
                <TextBlock Text="Tên món:" FontWeight="SemiBold" Margin="0,10,0,5" Foreground="#1E1B4B"/>
                <TextBox x:Name="txtName" Style="{StaticResource GlassTextStyleInput}" Margin="0,0,0,10"/>

                <TextBlock Text="Giá bán:" FontWeight="SemiBold" Margin="0,10,0,5" Foreground="#1E1B4B"/>
                <TextBox x:Name="txtPrice" Style="{StaticResource GlassTextStyleInput}" Margin="0,0,0,10"/>
            </StackPanel>

            <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,20,0,0">
                <Button Content="Lưu lại" Width="100"
                        Style="{StaticResource GlassButtonStyle}"
                        Click="BtnSave_Click"/>

                <Button Content="Hủy bỏ" Width="100" Margin="0,0,10,0"
                        Style="{StaticResource GlassDeleteButtonStyle}"
                        Click="BtnCancel_Click"/>
            </StackPanel>
        </Grid>
    </Border>
</Window>