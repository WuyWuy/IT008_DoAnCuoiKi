<Window x:Class="QuanLyCaPhe.Views.Admin.DetailWindow.InputInfoDetailWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:QuanLyCaPhe.Views.Admin.DetailWindow"
        mc:Ignorable="d"
        Title="Phiếu Nhập Kho" Height="480" Width="450"
        WindowStartupLocation="CenterScreen" ResizeMode="NoResize"
        Background="{StaticResource BackgroundLight}">

    <Border Style="{StaticResource LiquidGlassStyle}" Padding="20" Margin="10">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <TextBlock Text="PHIẾU NHẬP KHO" 
                       HorizontalAlignment="Center" 
                       FontSize="22" FontWeight="Bold" 
                       Foreground="{StaticResource PrimaryColor}" 
                       Margin="0,0,0,20"/>

            <StackPanel Grid.Row="1" VerticalAlignment="Top">

                <TextBlock Text="Chọn nguyên liệu/vật dụng:" FontWeight="SemiBold" Margin="0,10,0,5" Foreground="#1E1B4B"/>
                <ComboBox x:Name="cboIngredient" 
                          Style="{StaticResource ComboBoxStyle}"
                          DisplayMemberPath="IngName" 
                          SelectedValuePath="Id"/>

                <TextBlock Text="Ngày nhập:" FontWeight="SemiBold" Margin="0,10,0,5" Foreground="#1E1B4B"/>
                <DatePicker x:Name="dpDateInput" Height="36" 
                            VerticalContentAlignment="Center"
                            BorderThickness="0" Background="White">
                    <DatePicker.Resources>
                        <Style TargetType="DatePickerTextBox">
                            <Setter Property="Control.Template">
                                <Setter.Value>
                                    <ControlTemplate>
                                        <TextBox x:Name="PART_TextBox" 
                                                 Text="{Binding Path=SelectedDate, StringFormat='dd/MM/yyyy', RelativeSource={RelativeSource AncestorType={x:Type DatePicker}}}" 
                                                 Style="{StaticResource GlassTextStyleInput}"/>
                                    </ControlTemplate>
                                </Setter.Value>
                            </Setter>
                        </Style>
                    </DatePicker.Resources>
                </DatePicker>

                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>
                    <StackPanel Grid.Column="0" Margin="0,0,10,0">
                        <TextBlock Text="Số lượng nhập:" FontWeight="SemiBold" Margin="0,10,0,5" Foreground="#1E1B4B"/>
                        <TextBox x:Name="txtCount" Style="{StaticResource GlassTextStyleInput}"/>
                    </StackPanel>

                    <StackPanel Grid.Column="1">
                        <TextBlock Text="Đơn vị:" FontWeight="SemiBold" Margin="0,10,0,5" Foreground="#1E1B4B"/>
                        <TextBox x:Name="txtUnit" Width="100" IsReadOnly="True" Background="#F3F4F6" Style="{StaticResource GlassTextStyleInput}"/>
                    </StackPanel>
                </Grid>

                <TextBlock Text="Giá nhập (Tổng tiền lô hàng):" FontWeight="SemiBold" Margin="0,10,0,5" Foreground="#1E1B4B"/>
                <TextBox x:Name="txtInputPrice" Style="{StaticResource GlassTextStyleInput}"/>

            </StackPanel>

            <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,20,0,0">
                <Button Content="Nhập hàng" Width="100"
                        Style="{StaticResource GlassButtonStyle}"
                        Click="BtnSave_Click"/>

                <Button Content="Hủy bỏ" Width="100" Margin="0,0,10,0"
                        Style="{StaticResource GlassDeleteButtonStyle}"
                        Click="BtnCancel_Click"/>
            </StackPanel>
        </Grid>
    </Border>
</Window>