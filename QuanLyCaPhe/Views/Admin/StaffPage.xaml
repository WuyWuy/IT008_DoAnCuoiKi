<Page x:Class="QuanLyCaPhe.Views.Admin.StaffPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:QuanLyCaPhe.Views.Admin"
      xmlns:comp="clr-namespace:QuanLyCaPhe.Views.Components"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="StaffPage">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="0.1*"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Grid Grid.Row="0">
            <StackPanel Orientation="Horizontal">
                <comp:CrudToolBar VerticalAlignment="Bottom" Margin="3, 3, 20, 3"/>
                <comp:ExcelControl VerticalAlignment="Bottom" Margin="3"/>

            </StackPanel>

            <comp:SearchBar HorizontalAlignment="Right" VerticalAlignment="Bottom" Margin="3"/>
        </Grid>

        <DataGrid x:Name="Staffsdg" Grid.Row="1"
                  AutoGenerateColumns="False" CanUserAddRows="False" IsReadOnly="True" SelectionMode="Single"
                  Style="{StaticResource CardStyleDataGridStyle}">
            <DataGrid.Columns>
                <DataGridTextColumn Header="STT" Binding="{Binding Id}" Width="auto"/>
                <DataGridTextColumn Header="Họ và tên" Binding="{Binding FullName}" Width="auto"/>
                <DataGridTextColumn Header="Địa chỉ" Binding="{Binding Address}" Width="auto"/>
                <DataGridTextColumn Header="Số Điện thoại" Binding="{Binding Phone}" Width="auto"/>
                <DataGridTextColumn Header="Email" Binding="{Binding Email}" Width="auto"/>
                <DataGridTextColumn Header="Chức vụ" Binding="{Binding RoleName}" Width="auto"/>
            </DataGrid.Columns>

        </DataGrid>

    </Grid>
</Page>
