<Page x:Class="QuanLyCaPhe.Views.Admin.LastBillsPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:QuanLyCaPhe.Views.Admin"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="LastBillsPage">

    <Grid>
        <Border Padding="15">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>

                <Border Style="{StaticResource LiquidGlassStyle}"
                        Padding="15"
                        CornerRadius="10">
                    <Grid>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="*"/>
                        </Grid.RowDefinitions>

                        <TextBlock Grid.Row="0" Text="📊 HOẠT ĐỘNG GẦN ĐÂY" 
                           FontSize="16" 
                           FontWeight="Bold" 
                           Foreground="#1E1B4B" 
                           Margin="0,0,0,10"/>

                        <ListBox x:Name="lbRecentActivities" 

                                 Grid.Row="1"
                                 Background="Transparent"
                                 BorderThickness="0"
                                 ScrollViewer.HorizontalScrollBarVisibility="Disabled"
                                 ScrollViewer.VerticalScrollBarVisibility="Auto"
                                 Style="{StaticResource ActivityListBoxStyle}"
                                 ItemContainerStyle="{StaticResource ActivityItemStyle}">

                            <ListBox.ItemTemplate>
                                <DataTemplate>
                                    <Grid Margin="0, 4, 0, 4">
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="30"/>
                                            <ColumnDefinition Width="*"/>
                                            <ColumnDefinition Width="auto"/>
                                        </Grid.ColumnDefinitions>

                                        <Path Grid.Column="0" 
                                              Data="{Binding IconPath}" 
                                              Fill="{Binding IconColor}"
                                              Stretch="Uniform" Width="20" Height="20" 
                                              VerticalAlignment="Center" HorizontalAlignment="Center"/>

                                        <StackPanel Grid.Column="1" VerticalAlignment="Center" Margin="10,0,0,0">
                                            <TextBlock Text="{Binding Description}" 
                                               FontSize="14" 
                                               FontWeight="SemiBold" 
                                               Foreground="#1E1B4B"/>
                                            <TextBlock Text="{Binding Detail}" 
                                               FontSize="11" 
                                               Foreground="#64748B"/>
                                        </StackPanel>

                                        <TextBlock Grid.Column="2" 
                                                   Text="{Binding TimeAgo}" 
                                                   FontSize="12" 
                                                   Foreground="#64748B" 
                                                   VerticalAlignment="Center" 
                                                   HorizontalAlignment="Right"/>
                                    </Grid>
                                </DataTemplate>
                            </ListBox.ItemTemplate>
                        </ListBox>
                    </Grid>
                </Border>
            </Grid>
        </Border>
    </Grid>
</Page>
