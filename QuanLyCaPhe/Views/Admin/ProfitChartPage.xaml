<Page x:Class="QuanLyCaPhe.Views.Admin.ProfitChartPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:lvc="clr-namespace:LiveCharts.Wpf;assembly=LiveCharts.Wpf"
      Title="Biểu đồ doanh thu">

    <Page.Resources>
        <DropShadowEffect x:Key="CardShadow" BlurRadius="15" ShadowDepth="3" Opacity="0.12" Color="Black"/>
        <DropShadowEffect x:Key="HeavyShadow" BlurRadius="25" ShadowDepth="5" Opacity="0.2" Color="Black"/>

        <Style x:Key="HoverChartBorderStyle" TargetType="Border">
            <Setter Property="Effect" Value="{StaticResource CardShadow}"/>
            <Setter Property="Background" Value="White"/>
            <Setter Property="CornerRadius" Value="12"/>
            <Setter Property="BorderBrush" Value="#E5E7EB"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="25"/>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Effect" Value="{StaticResource HeavyShadow}"/>
                    <Setter Property="BorderBrush" Value="#3B82F6"/>
                </Trigger>
            </Style.Triggers>
        </Style>
    </Page.Resources>

    <Grid Background="Transparent">
        <Border Margin="2" Style="{StaticResource HoverChartBorderStyle}">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>

                <StackPanel Grid.Row="0" Margin="0,0,0,20">
                    <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                        <TextBlock Text="📊 " FontSize="18"/>
                        <TextBlock Text="BIỂU ĐỒ LỢI NHUẬN" 
                                   FontSize="16" FontWeight="Bold"
                                   Foreground="#374151" VerticalAlignment="Center"/>
                    </StackPanel>

                    <TextBlock Text="{Binding UnitText}"
                               FontSize="13" Foreground="#6B7280"
                               FontStyle="Italic"
                               Margin="26,2,0,0"/>
                </StackPanel>

                <lvc:CartesianChart Grid.Row="1"
                                    Series="{Binding ThreeLineSeries}"
                                    LegendLocation="None"
                                    Hoverable="True"
                                    DisableAnimations="False"
                                    AnimationsSpeed="0:0:0.5">

                    <lvc:CartesianChart.AxisX>
                        <lvc:Axis Labels="{Binding Labels}"
                                  Foreground="#6B7280"
                                  FontSize="12"
                                  Separator="{x:Static lvc:DefaultAxes.CleanSeparator}">
                        </lvc:Axis>
                    </lvc:CartesianChart.AxisX>

                    <lvc:CartesianChart.AxisY>
                        <lvc:Axis LabelFormatter="{Binding CurrencyFormatter}"
                                  Foreground="#6B7280"
                                  FontSize="12"/>
                    </lvc:CartesianChart.AxisY>

                    <lvc:CartesianChart.DataTooltip>
                        <lvc:DefaultTooltip BulletSize="10" 
                                            Background="#1F2937"
                                            Foreground="White"
                                            FontSize="14"
                                            SelectionMode="SharedXValues"/>
                    </lvc:CartesianChart.DataTooltip>

                </lvc:CartesianChart>
            </Grid>
        </Border>
    </Grid>
</Page>