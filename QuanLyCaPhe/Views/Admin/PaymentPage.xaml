<Page x:Class="QuanLyCaPhe.Views.Admin.PaymentPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      mc:Ignorable="d" 
      d:DesignHeight="500" d:DesignWidth="900"
      Title="PaymentPage">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Border Grid.Row="0" Margin="10, 10, 0, 0">
            <StackPanel Orientation="Horizontal">
                <TextBlock Text="&#xE8C7;" Style="{StaticResource PageHeaderIcon}"/>
                <TextBlock Text="QUẢN LÝ TÀI KHOẢN THANH TOÁN" Style="{StaticResource PageHeaderTitle}"/>
            </StackPanel>
        </Border>

        <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto" Padding="10" HorizontalAlignment="Center">
            <ItemsControl x:Name="icPayments">
                <ItemsControl.ItemsPanel>
                    <ItemsPanelTemplate>
                        <WrapPanel Orientation="Horizontal" ItemWidth="360" ItemHeight="210"/>

                    </ItemsPanelTemplate>
                </ItemsControl.ItemsPanel>
                
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <Border Margin="10" CornerRadius="12" BorderThickness="2" Background="White">
                            <Border.Effect>
                                <DropShadowEffect Color="#E2E8F0" BlurRadius="15" ShadowDepth="3" Opacity="0.5"/>
                            </Border.Effect>

                            <Border.Style>
                                <Style TargetType="Border">
                                    <Setter Property="BorderBrush" Value="#E5E7EB"/>
                                    <Style.Triggers>
                                        <DataTrigger Binding="{Binding IsActive}" Value="True">
                                            <Setter Property="BorderBrush" Value="{StaticResource TextSecondary}"/>
                                            <Setter Property="BorderThickness" Value="2.5"/>
                                            <Setter Property="Background" Value="#F0FDF4"/>
                                        </DataTrigger>
                                    </Style.Triggers>
                                </Style>
                            </Border.Style>

                            <Border Padding="20">
                                <Grid>
                                    <Grid.RowDefinitions>
                                        <RowDefinition Height="Auto"/>
                                        <RowDefinition Height="*"/>
                                        <RowDefinition Height="Auto"/>
                                        <RowDefinition Height="Auto"/>
                                    </Grid.RowDefinitions>

                                    <Grid Grid.Row="0">
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="*"/>
                                            <ColumnDefinition Width="Auto"/>
                                        </Grid.ColumnDefinitions>

                                        <StackPanel Orientation="Horizontal">
                                            <Border Width="40" Height="40" CornerRadius="20" Background="#F3F4F6" Margin="0,0,10,0">
                                                <TextBlock Text="&#xE8C7;" FontFamily="Segoe MDL2 Assets" 
                                                           VerticalAlignment="Center" HorizontalAlignment="Center" Foreground="#64748B"/>
                                            </Border>
                                            <StackPanel VerticalAlignment="Center">
                                                <TextBlock Text="{Binding BankName}" FontWeight="Bold" FontSize="16" Foreground="#1E293B"/>
                                                <TextBlock Text="{Binding BankBin, StringFormat='BIN: {0}'}" FontSize="12" Foreground="#94A3B8"/>
                                            </StackPanel>
                                        </StackPanel>

                                        <Button Grid.Column="1" Style="{StaticResource GlassButtonStyle}"
                                                Width="30" Height="30" Padding="0"
                                                ToolTip="Tùy chọn"
                                                Click="BtnOpenMenu_Click">

                                            <TextBlock Text="&#xE712;" FontFamily="Segoe MDL2 Assets" FontSize="14" Foreground="#64748B"/>

                                            <Button.ContextMenu>
                                                <ContextMenu Style="{DynamicResource GlassContextMenu}">
                                                    <MenuItem Header="Sửa thông tin" Click="BtnEdit_Click">
                                                        <MenuItem.Icon>
                                                            <TextBlock Text="&#xE70F;" FontFamily="Segoe MDL2 Assets"/>
                                                        </MenuItem.Icon>
                                                    </MenuItem>

                                                    <MenuItem Header="Xóa tài khoản" Click="BtnDelete_Click" Foreground="#EF4444">
                                                        <MenuItem.Icon>
                                                            <TextBlock Text="&#xE74D;" FontFamily="Segoe MDL2 Assets" Foreground="#EF4444"/>
                                                        </MenuItem.Icon>
                                                    </MenuItem>
                                                </ContextMenu>
                                            </Button.ContextMenu>
                                            
                                        </Button>
                                    </Grid>

                                    <TextBlock Grid.Row="1" Text="{Binding AccountNumber}" 
                                               VerticalAlignment="Center"
                                               FontFamily="Consolas" FontSize="24" FontWeight="Bold" 
                                               Foreground="#334155"/>

                                    <StackPanel Grid.Row="2" Margin="0,0,0,15">
                                        <TextBlock Text="CHỦ TÀI KHOẢN" FontSize="10" Foreground="#94A3B8" FontWeight="SemiBold"/>
                                        <TextBlock Text="{Binding AccountName}" FontSize="14" FontWeight="Bold" Foreground="#475569" TextTrimming="CharacterEllipsis"/>
                                    </StackPanel>

                                    <Grid Grid.Row="3">
                                        <Button Content="Sử dụng tài khoản này"
            Height="36"
            Click="BtnActive_Click" Tag="{Binding Id}">
                                            <Button.Style>
                                                <Style TargetType="Button" BasedOn="{StaticResource GlassButtonStyle}">
                                                    <Style.Triggers>
                                                        <DataTrigger Binding="{Binding IsActive}" Value="True">
                                                            <Setter Property="Visibility" Value="Collapsed"/>
                                                        </DataTrigger>
                                                    </Style.Triggers>
                                                </Style>
                                            </Button.Style>
                                        </Button>

                                        <Border Background="{StaticResource TextPrimary}" CornerRadius="6" Height="36">
                                            <Border.Style>
                                                <Style TargetType="Border">
                                                    <Setter Property="Visibility" Value="Collapsed"/>
                                                    <Style.Triggers>
                                                        <DataTrigger Binding="{Binding IsActive}" Value="True">
                                                            <Setter Property="Visibility" Value="Visible"/>
                                                        </DataTrigger>
                                                    </Style.Triggers>
                                                </Style>
                                            </Border.Style>

                                            <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" VerticalAlignment="Center">
                                                <TextBlock Text="&#xE73E;" FontFamily="Segoe MDL2 Assets" Foreground="White" Margin="0,0,8,0" FontSize="14"/>
                                                <TextBlock Text="ĐANG SỬ DỤNG" Foreground="White" FontWeight="Bold" FontSize="12"/>
                                            </StackPanel>
                                        </Border>
                                    </Grid>
                                </Grid>
                            </Border>
                        </Border>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </ScrollViewer>

        <Button Grid.Row="1" Style="{StaticResource FabButtonStyle}"
                HorizontalAlignment="Right" VerticalAlignment="Bottom"
                Margin="0,0,20,20"
                Click="BtnAdd_Click">
            <TextBlock Text="&#xE710;" FontFamily="Segoe MDL2 Assets" FontSize="20" FontWeight="Bold" Foreground="White"/>
        </Button>
    </Grid>
</Page>