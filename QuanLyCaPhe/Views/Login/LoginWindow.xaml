<Window x:Class="QuanLyCaPhe.Views.Login.LoginWindow"
        xmlns:local="clr-namespace:QuanLyCaPhe"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Login" Height="500" Width="400" WindowStartupLocation="CenterScreen"
        Background="SkyBlue"
        WindowStyle="None">
    <Grid Margin="12">
        <Grid Margin="12">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="1*"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <StackPanel Grid.Row="0" Margin="0,0,0,8">
                <TextBlock Text="Login" 
                        FontSize="60"
                        HorizontalAlignment="Center"
                        Style="{StaticResource LoginHeaderStyle}"/>
            </StackPanel>

            <StackPanel Grid.Row="2" Margin="0,0,0,8" HorizontalAlignment="Center">
                <TextBlock Text="Email" />
                <TextBox x:Name="txtLoginEmail" Width="300" 
                      HorizontalAlignment="Left" 
                      Margin="0, 0, 10, 0"
                      Style="{StaticResource LoginTextBoxStyle}"/>
            </StackPanel>

            <StackPanel Grid.Row="3" Margin="0,0,0,8" HorizontalAlignment="Center">
                <TextBlock Text="Password" />
                <PasswordBox x:Name="pbLoginPassword"  Width="300" 
                      HorizontalAlignment="Left" 
                      Margin="0, 0, 10, 0" 
                      Style="{StaticResource LoginPasswordBoxStyle}"/>
            </StackPanel>

            <TextBlock Grid.Row="4" x:Name="tbLoginStatus" Foreground="Red" TextWrapping="Wrap" />

            <StackPanel Grid.Row="5" Orientation="Horizontal" Margin="0,8,0,8" HorizontalAlignment="Center" >
                <Button x:Name="btnLogin" Width="120" Click="BtnLogin_Click"
                     Content="Login"
                     Style="{StaticResource LoginButtonStyle}"/>
            </StackPanel>


        </Grid>
        <Border BorderBrush="Black" BorderThickness="1"/>

    </Grid>
</Window>